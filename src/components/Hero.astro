---
// Hero section — full screen with floating orbs and animated background
---

<section class="hero" aria-label="Apresentação da Projekt">
  <!-- Abstract background -->
  <div class="hero-bg" aria-hidden="true">
    <!-- Floating orbs -->
    <div class="orb orb-violet" style="width:600px;height:600px;top:-200px;left:-200px;"></div>
    <div class="orb orb-blue" style="width:500px;height:500px;top:100px;right:-150px;"></div>
    <div class="orb orb-neon" style="width:300px;height:300px;bottom:0;left:40%;"></div>

    <!-- Grid dot pattern -->
    <div class="hero-grid-overlay"></div>

    <!-- Decorative lines -->
    <div class="hero-lines" aria-hidden="true">
      <div class="hero-line hero-line-1"></div>
      <div class="hero-line hero-line-2"></div>
    </div>
  </div>

  <!-- Hero content -->
  <div class="hero-content-wrapper">
    <div class="hero-content">
      <!-- Left column -->
      <div class="hero-left">
        <!-- Tag chip -->
        <div class="hero-chip reveal reveal-delay-1">
          <span class="chip-dot" aria-hidden="true"></span>
          <span class="mono">// consultoria &amp; inovação</span>
        </div>

        <!-- Main heading -->
        <h1 class="hero-title reveal reveal-delay-2">
          Tecnologia que
          <br />
          <span class="gradient-text">transforma</span>.
          <br />
          Resultados que
          <br />
          <span class="gradient-text">importam</span>.
        </h1>

        <!-- Subheading -->
        <p class="hero-subtitle reveal reveal-delay-3">
          Da descoberta ao deploy — arquitetamos soluções de tecnologia e IA para empresas que querem crescer com inteligência e segurança.
        </p>

        <!-- CTAs -->
        <div class="hero-ctas reveal reveal-delay-4">
          <a
            href="/contato"
            class="btn-primary"
            style="padding:0.875rem 2rem;font-size:1rem;"
          >
            Iniciar Projeto
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a
            href="https://wa.me/5541999999999"
            target="_blank"
            rel="noopener noreferrer"
            class="btn-secondary"
            style="padding:0.875rem 2rem;font-size:1rem;"
          >
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413z"/>
            </svg>
            Ver no WhatsApp
          </a>
        </div>
      </div>

      <!-- Right column: abstract decoration -->
      <div class="hero-right" aria-hidden="true">
        <div class="hero-decoration">
          <!-- Abstract tech visual -->
          <div class="hero-code-block">
            <div class="code-header">
              <span class="code-dot" style="background:#ff5f57;"></span>
              <span class="code-dot" style="background:#febc2e;"></span>
              <span class="code-dot" style="background:#28c840;"></span>
              <span class="code-filename">projekt.config.ts</span>
            </div>
            <pre class="code-content"><code><span class="token-keyword">const</span> <span class="token-name">config</span> = &#123;
  <span class="token-key">discovery</span>: <span class="token-string">"structured"</span>,
  <span class="token-key">ai</span>: <span class="token-bool">true</span>,
  <span class="token-key">delivery</span>: <span class="token-string">"on-time"</span>,
  <span class="token-key">stack</span>: [
    <span class="token-string">"cloud"</span>,
    <span class="token-string">"ai"</span>,
    <span class="token-string">"modern"</span>,
  ],
  <span class="token-key">results</span>: <span class="token-string">"guaranteed"</span>,
&#125;</code></pre>
          </div>
          <!-- Floating metric card -->
          <div class="metric-card metric-card-1">
            <span class="metric-value">100%</span>
            <span class="metric-label">Remote-ready</span>
          </div>
          <div class="metric-card metric-card-2">
            <span class="metric-value">15+</span>
            <span class="metric-label">Projetos entregues</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Stats strip -->
  <div class="hero-stats">
    <div class="hero-stats-inner">
      <div class="stat-item">
        <span class="stat-number">15+</span>
        <span class="stat-label">projetos entregues</span>
      </div>
      <div class="stat-divider" aria-hidden="true"></div>
      <div class="stat-item">
        <span class="stat-number">100%</span>
        <span class="stat-label">remote-ready</span>
      </div>
      <div class="stat-divider" aria-hidden="true"></div>
      <div class="stat-item">
        <span class="stat-number mono" style="font-size:1rem;">BR</span>
        <span class="stat-label">Curitiba &amp; Brasil</span>
      </div>
      <div class="stat-divider" aria-hidden="true"></div>
      <div class="stat-item">
        <span class="stat-number">IA</span>
        <span class="stat-label">integração nativa</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: calc(100vh - var(--header-height));
    display: flex;
    flex-direction: column;
    overflow: hidden;
    background: var(--bg);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }

  .hero-grid-overlay {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle, var(--border) 1px, transparent 1px);
    background-size: 32px 32px;
    opacity: 0.5;
  }

  .hero-lines {
    position: absolute;
    inset: 0;
  }

  .hero-line {
    position: absolute;
    background: linear-gradient(90deg, transparent, rgba(var(--accent-rgb), 0.15), transparent);
    height: 1px;
  }

  .hero-line-1 {
    top: 30%;
    left: 0;
    right: 0;
  }

  .hero-line-2 {
    top: 65%;
    left: 0;
    right: 0;
  }

  .hero-content-wrapper {
    position: relative;
    z-index: 1;
    flex: 1;
    display: flex;
    align-items: center;
  }

  .hero-content {
    max-width: 1280px;
    margin: 0 auto;
    padding: 4rem 1.5rem 3rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    width: 100%;
  }

  .hero-left {
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
  }

  .hero-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.625rem;
    padding: 0.4rem 0.875rem;
    background: rgba(var(--accent-rgb), 0.08);
    border: 1px solid rgba(var(--accent-rgb), 0.25);
    border-radius: 999px;
    width: fit-content;
  }

  .chip-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--accent-neon);
    animation: pulse-glow 2s ease-in-out infinite;
    display: inline-block;
  }

  .hero-title {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: clamp(2.5rem, 5.5vw, 4.5rem);
    line-height: 1.08;
    letter-spacing: -0.04em;
    color: var(--text);
  }

  .hero-subtitle {
    font-size: clamp(1rem, 1.5vw, 1.125rem);
    color: var(--text-muted);
    line-height: 1.75;
    max-width: 520px;
  }

  .hero-ctas {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Right: decoration */
  .hero-right {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-decoration {
    position: relative;
    width: 100%;
    max-width: 440px;
  }

  .hero-code-block {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    animation: float 8s ease-in-out infinite;
  }

  .code-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: var(--bg-alt);
    border-bottom: 1px solid var(--border);
  }

  .code-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .code-filename {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-left: auto;
  }

  .code-content {
    padding: 1.25rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8125rem;
    line-height: 1.8;
    color: var(--text-muted);
    margin: 0;
    overflow-x: auto;
    background: transparent;
    border: none;
  }

  .token-keyword { color: var(--accent); font-weight: 500; }
  .token-name { color: var(--accent-2); }
  .token-key { color: var(--text); }
  .token-string { color: var(--accent-neon); }
  .token-bool { color: #f59e0b; }

  /* Floating metric cards */
  .metric-card {
    position: absolute;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 0.75rem 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    box-shadow: var(--shadow-lg);
    backdrop-filter: blur(10px);
  }

  .metric-card-1 {
    top: -20px;
    right: -30px;
    animation: float 6s ease-in-out infinite 1s;
  }

  .metric-card-2 {
    bottom: -20px;
    left: -20px;
    animation: float 7s ease-in-out infinite 3s;
  }

  .metric-value {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1.375rem;
    color: var(--accent);
    letter-spacing: -0.02em;
  }

  .metric-label {
    font-size: 0.75rem;
    color: var(--text-muted);
    font-weight: 500;
  }

  /* Stats strip */
  .hero-stats {
    position: relative;
    z-index: 1;
    border-top: 1px solid var(--border);
    background: var(--surface);
  }

  .hero-stats-inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0;
    flex-wrap: wrap;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.2rem;
    padding: 0.5rem 2.5rem;
  }

  .stat-number {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1.5rem;
    color: var(--accent);
    letter-spacing: -0.03em;
  }

  .stat-label {
    font-size: 0.8rem;
    color: var(--text-muted);
    font-weight: 400;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: var(--border);
  }

  @media (max-width: 900px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      text-align: center;
    }

    .hero-left {
      align-items: center;
    }

    .hero-subtitle {
      text-align: center;
    }

    .hero-right {
      display: none;
    }

    .stat-item {
      padding: 0.5rem 1.25rem;
    }
  }

  @media (max-width: 560px) {
    .stat-divider {
      display: none;
    }

    .hero-stats-inner {
      gap: 0.5rem;
    }

    .stat-item {
      padding: 0.5rem 1rem;
    }
  }
</style>

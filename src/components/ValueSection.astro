---
// ValueSection — "Como geramos valor" — inverted background with outcomes
const outcomes = [
  {
    title: 'Redução do tempo de discovery',
    description: 'Metodologia estruturada elimina o ciclo de tentativa e erro. Você começa a construir com clareza, não com suposições.',
  },
  {
    title: 'Previsibilidade em projetos de tecnologia',
    description: 'Estimativas realistas, entregas incrementais e visibilidade completa do progresso a cada sprint.',
  },
  {
    title: 'Arquitetura baseada em contexto de negócio',
    description: 'Decisões técnicas fundamentadas em dados reais, não em tendências. A tecnologia certa para o problema certo.',
  },
  {
    title: 'IA que amplifica, não substitui',
    description: 'Integramos IA sem destruir processos humanos. O resultado é uma equipe mais capaz, não substituída por algoritmos.',
  },
  {
    title: 'Entregáveis claros do briefing ao go-live',
    description: 'Cada etapa tem artefatos documentados e aceites formais. Nenhuma surpresa no final do projeto.',
  },
];

const stats = [
  { value: '12', unit: 'semanas', label: 'de discovery ao deploy' },
  { value: '4', unit: 'etapas', label: 'no Projekt Loop' },
  { value: '∞', unit: 'iterações', label: 'de melhoria contínua' },
];
---

<section class="value-section section" aria-labelledby="value-title">
  <div class="container" style="max-width:1280px;margin:0 auto;padding:0 1.5rem;">
    <div class="value-grid">
      <!-- Left: heading and stats -->
      <div class="value-left reveal">
        <span class="section-label">Por que a Projekt</span>
        <h2 id="value-title" class="section-title" style="color:var(--text);">
          Como geramos <span class="gradient-text">valor real</span>
        </h2>
        <p style="color:var(--text-muted);line-height:1.75;margin-bottom:2.5rem;">
          Não entregamos código. Entregamos resultados de negócio sustentados por tecnologia bem construída.
        </p>

        <!-- Stats -->
        <div class="value-stats">
          {stats.map((stat) => (
            <div class="value-stat">
              <div class="value-stat-number">
                <span class="counter" data-value={stat.value}>{stat.value}</span>
                <span class="value-stat-unit">{stat.unit}</span>
              </div>
              <span class="value-stat-label">{stat.label}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- Right: outcomes list -->
      <div class="value-right">
        <ul class="outcomes-list" role="list">
          {outcomes.map((outcome, i) => (
            <li class={`outcome-item reveal reveal-delay-${i + 1}`}>
              <div class="outcome-icon" aria-hidden="true">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="20 6 9 17 4 12"/>
                </svg>
              </div>
              <div class="outcome-text">
                <strong class="outcome-title">{outcome.title}</strong>
                <p class="outcome-description">{outcome.description}</p>
              </div>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>

  <!-- Decorative orbs -->
  <div class="value-orbs" aria-hidden="true">
    <div class="orb orb-violet" style="width:400px;height:400px;bottom:-200px;right:-100px;opacity:0.15;"></div>
  </div>
</section>

<style>
  .value-section {
    background: var(--bg-alt);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    position: relative;
    overflow: hidden;
  }

  .value-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 5rem;
    align-items: start;
  }

  .value-left {
    position: sticky;
    top: calc(var(--header-height) + 2rem);
  }

  .value-stats {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
  }

  .value-stat {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .value-stat-number {
    display: flex;
    align-items: baseline;
    gap: 0.375rem;
  }

  .counter {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 2.5rem;
    color: var(--accent);
    letter-spacing: -0.04em;
    line-height: 1;
  }

  .value-stat-unit {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.875rem;
    color: var(--text-muted);
    font-weight: 500;
  }

  .value-stat-label {
    font-size: 0.8125rem;
    color: var(--text-muted);
  }

  /* Outcomes list */
  .outcomes-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .outcome-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem 0;
    border-bottom: 1px solid var(--border);
  }

  .outcome-item:last-child {
    border-bottom: none;
  }

  .outcome-icon {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(var(--accent-neon-rgb), 0.12);
    border: 1px solid rgba(var(--accent-neon-rgb), 0.3);
    border-radius: 50%;
    color: var(--accent-neon);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .outcome-text {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
    flex: 1;
  }

  .outcome-title {
    font-family: 'Syne', sans-serif;
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.01em;
  }

  .outcome-description {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.65;
    margin: 0;
  }

  .value-orbs {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  @media (max-width: 900px) {
    .value-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .value-left {
      position: static;
    }

    .value-stats {
      flex-direction: row;
      gap: 1rem;
    }

    .value-stat {
      flex: 1;
    }

    .counter {
      font-size: 2rem;
    }
  }

  @media (max-width: 560px) {
    .value-stats {
      flex-direction: column;
    }
  }
</style>

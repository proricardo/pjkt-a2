---
import Layout from '@/layouts/Layout.astro';
import Breadcrumb from '@/components/Breadcrumb.astro';
import ContactSection from '@/components/ContactSection.astro';

const breadcrumbs = [
  { label: 'Início', href: '/' },
  { label: 'Contato', href: '/contato' },
];

const siteUrl = 'https://projekt.com.br';

const localBusinessSchema = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: 'Projekt',
  description: 'Consultoria de Tecnologia e IA para empresas que querem crescer com inteligência e segurança.',
  url: siteUrl,
  email: 'contato@projekt.com.br',
  address: {
    '@type': 'PostalAddress',
    addressLocality: 'Curitiba',
    addressRegion: 'PR',
    addressCountry: 'BR',
  },
  areaServed: 'BR',
  knowsLanguage: 'pt-BR',
  priceRange: '$$',
};
---

<Layout
  title="Contato"
  description="Entre em contato com a Projekt. Preencha o formulário, envie um email ou fale via WhatsApp. Retornamos em até 24 horas úteis."
>
  <!-- LocalBusiness Schema -->
  <script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />

  <!-- Hero -->
  <section style="background:var(--bg);padding:3rem 0 0;">
    <div class="container" style="max-width:1280px;margin:0 auto;padding:0 1.5rem;">
      <Breadcrumb items={breadcrumbs} />
      <div style="max-width:640px;margin-top:2rem;padding-bottom:3rem;">
        <span class="section-label">Fale conosco</span>
        <h1 class="section-title gradient-text">Vamos conversar?</h1>
        <p class="section-subtitle">
          Seja para iniciar um projeto, tirar uma dúvida ou entender se a Projekt é o parceiro certo para você — estamos aqui.
        </p>
      </div>
    </div>
  </section>

  <!-- Contact section (reused component) -->
  <ContactSection />

  <!-- Additional info section -->
  <section style="background:var(--bg-alt);border-top:1px solid var(--border);padding:4rem 0;">
    <div class="container" style="max-width:1280px;margin:0 auto;padding:0 1.5rem;">
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:1.5rem;">
        <div class="info-block reveal">
          <div class="info-block-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12 6 12 12 16 14"/>
            </svg>
          </div>
          <h3 class="info-block-title">Tempo de resposta</h3>
          <p class="info-block-text">
            Respondemos todas as mensagens em até 24 horas úteis. Para urgências, use o WhatsApp.
          </p>
        </div>

        <div class="info-block reveal reveal-delay-2">
          <div class="info-block-icon" style="background:rgba(var(--accent-2-rgb),0.08);border-color:rgba(var(--accent-2-rgb),0.2);color:var(--accent-2);">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <h3 class="info-block-title">Primeira conversa</h3>
          <p class="info-block-text">
            A primeira conversa é gratuita e sem compromisso. Queremos entender seu desafio antes de qualquer proposta.
          </p>
        </div>

        <div class="info-block reveal reveal-delay-3">
          <div class="info-block-icon" style="background:rgba(var(--accent-neon-rgb),0.08);border-color:rgba(var(--accent-neon-rgb),0.2);color:var(--accent-neon);">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3 class="info-block-title">Atendimento remoto</h3>
          <p class="info-block-text">
            Trabalhamos remotamente em todo o Brasil. Sediados em Curitiba, atendemos clientes de norte a sul.
          </p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .info-block {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .info-block-icon {
    width: 52px;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(var(--accent-rgb), 0.08);
    border: 1px solid rgba(var(--accent-rgb), 0.2);
    border-radius: 14px;
    color: var(--accent);
  }

  .info-block-title {
    font-family: 'Syne', sans-serif;
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--text);
    margin: 0;
    letter-spacing: -0.02em;
  }

  .info-block-text {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.65;
    margin: 0;
  }

  @media (max-width: 768px) {
    div[style*="grid-template-columns:repeat(3,1fr)"] {
      grid-template-columns: 1fr !important;
    }
  }
</style>
